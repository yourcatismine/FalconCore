# ─────────────────────────────────────────────────────────────
# FalconCore - AntiCheat: Scaffold Detection Configuration
# ─────────────────────────────────────────────────────────────

enabled: true

violations:
  alert-threshold: 1.0
  punishment-threshold: 20.0

setback:
  enabled: true
  min-vl: 1.0

subchecks:
  type-a:
    enabled: true
    vl-increment: 1.5
  type-b:
    enabled: true
    max-pitch: 38.0
    vl-increment: 1.5
  type-c:
    enabled: true
    vl-increment: 1.5
  type-d:
    enabled: true
    vl-increment: 1.5
